@REM A .bat file, also known as a batch file, is a script file used in Windows operating systems to automate and streamline various tasks. These files contain a series of commands that are executed one after the other when the .bat file is run. Batch files are written in plain text and use the .bat file extension. 

node ./index.js